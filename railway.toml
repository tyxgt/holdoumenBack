[build]
builder = "python"
pythonVersion = "3.12"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"

[vars]
APP_ENV = "production"
DEBUG = "false"
LLM_PROVIDER = "openai"

[[services]]
name = "postgresql"
type = "postgresql"
image = "railway/postgresql:16"
